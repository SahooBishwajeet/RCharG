#ifndef TUI_H
#define TUI_H

#include <variable.h>

// Get the size of the final string
void getSize(randArray *ptr);

// Get the type of random characters
void getType(randArray *ptr);

// Produces the output random string
void setOutput(randArray *ptr);

#endif // TUI_H