#ifndef ARRAY_H
#define ARRAY_H

#include <variable.h>

// Insert character of all range
void charOptions(const char *option, randArray *ptr);

// Create characters of valid range 
void createArray(randArray *ptr);

#endif // ARRAY_H